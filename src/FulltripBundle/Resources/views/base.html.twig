<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>FullTrip | {% block title %}{% endblock %}</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.6.3/css/font-awesome.min.css">
    <link rel="stylesheet" href="{{ asset('css/main.css') }}">
    {% block stylesheets %}{% endblock %}
</head>
<body>
<header>
    <a href="/#">
        <img src="{{ asset('img/sm-logo.png') }}" alt="FullTrip logo" class="logo">
    </a>
    <img src="{{ asset('img/sm-menu.png') }}" alt="Mobile menu" class="sm-menu">

    <nav>
        <ul>
            <li><a href="#">Proposer une activité</a></li>
            <li><a href="#" id="login">Inscription / Connexion</a></li>
        </ul>
    </nav>
</header>

<div class="modal">
    <div class="mask"></div>
    <div class="login-popup">
        {% render url('fos_user_security_login') %}
    </div>
    {% if app.request.hasPreviousSession %}
        {% for type, messages in app.session.flashbag.all() %}
            {% for message in messages %}
                <div class="flash-{{ type }}">
                    {{ message }}
                </div>
            {% endfor %}
        {% endfor %}
    {% endif %}

    <div>
        {% block fos_user_content %}
        {% endblock fos_user_content %}
    </div>
</div>

<main role="main">
    {% block main %}{% endblock %}
</main>

<!--<footer>
    <span>&copy; 2016 FullTrip</span>
</footer>-->

<script src="https://ajax.googleapis.com/ajax/libs/jquery/2.2.3/jquery.min.js"></script>
<script src="{{ asset('js/modals.js') }}"></script>
<script src="{{ asset('js/login.js') }}"></script>
{% block javascripts %}{% endblock %}
</body>
</html>